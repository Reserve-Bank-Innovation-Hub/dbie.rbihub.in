# Two-package workspace: the app at the root, the dbie-mcp package in mcp/.
# pnpm >=10 also stores its build-script approvals here (allowBuilds).
packages:
  - '.'
  - 'mcp'

allowBuilds:
  es5-ext: true
  sharp: true
