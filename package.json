{
    "name": "dbie.rbihub.in",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "start": "pnpm --dir frontend start:frontend",
        "build": "pnpm --dir frontend build",
        "data:build": "npm --prefix data/processors run build",
        "data:verify": "npm --prefix data/processors run verify",
        "scrape": "node dbie-scraper/src/scrape-sdmx.mjs"
    }
}
