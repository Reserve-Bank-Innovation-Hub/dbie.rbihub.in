{
  "name": "dbie-processors",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Local processing step: parse raw DBIE source files into frontend-consumable JSON (uploaded to S3 site/).",
  "scripts": {
    "build": "node build.mjs",
    "verify": "node verify-all.mjs"
  },
  "dependencies": { "xlsx": "0.18.5" }
}
